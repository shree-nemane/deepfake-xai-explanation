# Frontend

React + Vite investigation UI for the forensic audio platform.

See the [root README](../README.md) for full setup. Quick start:

```powershell
npm install
npm run dev
```

Default API base: `http://localhost:8000` (configured in `src/App.jsx`).

Production build: `npm run build`
