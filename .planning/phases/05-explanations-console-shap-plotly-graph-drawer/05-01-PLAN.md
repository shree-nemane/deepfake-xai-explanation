---
phase: "05-explanations-console-shap-plotly-graph-drawer"
plan: "05-01"
title: "Explainability Drawer with SHAP, Sensitivity, and Plotly Graph"
type: "mvp"
wave: 1
depends_on: []
autonomous: true
requirements: ["UI-02"]
files_modified:
  - frontend/src/features/analysis/Dashboard.jsx
  - frontend/src/components/explainability/ExplainabilityDrawer.jsx
  - frontend/src/components/explainability/ShapContributionPanel.jsx
  - frontend/src/components/explainability/CounterfactualPanel.jsx
  - frontend/src/components/explainability/PlotlyEvidenceGraph.jsx
  - frontend/src/index.css
---

# Phase 5 Plan 05-01: Explainability Drawer

<objective>
Deliver UI-02 as a vertical MVP slice: an expandable dashboard drawer that renders SHAP Recharts bars, Level 1 analytical counterfactual sensitivity controls, and an interactive Plotly evidence graph from existing `AnalysisResponse.xai` payloads.
</objective>

<tasks>

<task id="1" name="Create ExplainabilityDrawer shell">
<type>execute</type>
<files>
- `frontend/src/components/explainability/ExplainabilityDrawer.jsx`
- `frontend/src/index.css`
</files>
<read_first>
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-UI-SPEC.md`
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-PATTERNS.md`
- `frontend/src/features/analysis/Uploader.jsx`
- `frontend/src/index.css`
</read_first>
<action>
Create `ExplainabilityDrawer.jsx` with props `open`, `onClose`, and `result`. Render a fixed right slide-over panel with backdrop, header title "Explainability Console", and close button using `framer-motion` and `lucide-react`. Add CSS classes `.explainability-drawer`, `.drawer-backdrop`, and `.drawer-header` in `index.css` matching existing glass/dark theme. Panel sections are placeholder slots for tasks 2–4.
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- `ExplainabilityDrawer.jsx` exports a default component accepting `open`, `onClose`, and `result` props.
- When `open` is false the drawer is not visible; when true it slides in from the right.
- Backdrop click calls `onClose`.
- `index.css` contains `.explainability-drawer` and `.drawer-backdrop`.
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="2" name="SHAP contribution panel with Recharts">
<type>execute</type>
<files>
- `frontend/src/components/explainability/ShapContributionPanel.jsx`
- `frontend/src/components/explainability/ExplainabilityDrawer.jsx`
</files>
<read_first>
- `frontend/src/features/analysis/AnalysisCharts.jsx`
- `backend/api/routes/analysis.py` (search `_build_xai_payload`)
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-CONTEXT.md`
</read_first>
<action>
Create `ShapContributionPanel.jsx` that reads `result.xai?.shap_values?.summary?.average_values` and maps each agent to `{ name, score }` where `score` is `Math.abs(value) * 100` for chart display. Reuse `ShapBarChart` from `AnalysisCharts.jsx`. Show `result.xai?.shap_summary` as caption text. Render empty state when no average values exist. Mount inside `ExplainabilityDrawer`.
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- `ShapContributionPanel.jsx` imports and uses `ShapBarChart`.
- Chart data is derived from `xai.shap_values.summary.average_values`.
- Empty state text reads "No Shapley attributions available for this report." when data is missing.
- `ExplainabilityDrawer.jsx` renders `ShapContributionPanel` with the `result` prop.
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="3" name="Level 1 counterfactual sensitivity panel">
<type>execute</type>
<files>
- `frontend/src/components/explainability/CounterfactualPanel.jsx`
- `frontend/src/components/explainability/ExplainabilityDrawer.jsx`
</files>
<read_first>
- `backend/explainability/counterfactuals/counterfactual_engine.py`
- `tests/xai/test_counterfactual_engine.py`
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-UI-SPEC.md`
- `frontend/src/components/forensic/FeatureAnalysisPanel.jsx`
</read_first>
<action>
Create `CounterfactualPanel.jsx` with chunk `<select>` over `result.xai?.counterfactuals?.chunks` (label: `Chunk {chunk_index} ({start_time}s–{end_time}s)`). For the selected chunk, render one row per agent in `sensitivities` with a range input 0–20 and display `statement`, `gradient`, and `direction`. Subtitle must read "Level 1 Analytical Sensitivity — not perturbation simulation". Do not recompute verdicts on the client. Mount in drawer.
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- Chunk selector lists all entries in `xai.counterfactuals.chunks`.
- Each agent row shows backend `statement` text from `sensitivities[agent].statement`.
- UI subtitle includes "Level 1 Analytical Sensitivity".
- No client-side consensus recomputation functions are added.
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="4" name="Interactive Plotly evidence graph">
<type>execute</type>
<files>
- `frontend/src/components/explainability/PlotlyEvidenceGraph.jsx`
- `frontend/src/components/explainability/ExplainabilityDrawer.jsx`
- `frontend/src/index.css`
</files>
<read_first>
- `backend/explainability/evidence_graph.py`
- `frontend/src/components/forensic/EvidenceGraph.jsx`
- `frontend/package.json`
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-RESEARCH.md`
</read_first>
<action>
Create `PlotlyEvidenceGraph.jsx` that lazy-imports `react-plotly.js`. Convert `result.xai?.evidence_graph.nodes` and `.edges` into a Plotly scatter+line figure: Y position from node `layer`, X from node index within layer, node hover shows `label` and `type`, edge lines connect `source` to `target`. Minimum height 320px. Dark layout colors matching dashboard. Empty state when no nodes. Mount in drawer below sensitivity panel.
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- `PlotlyEvidenceGraph.jsx` uses dynamic import or lazy load for `react-plotly.js`.
- Component accepts `evidenceGraph` prop and renders when `nodes.length > 0`.
- Empty state text reads "No evidence graph available." when nodes are absent.
- `ExplainabilityDrawer.jsx` passes `result.xai?.evidence_graph` to `PlotlyEvidenceGraph`.
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="5" name="Wire drawer into Dashboard">
<type>execute</type>
<files>
- `frontend/src/features/analysis/Dashboard.jsx`
- `frontend/src/components/explainability/ExplainabilityDrawer.jsx`
- `frontend/src/index.css`
</files>
<read_first>
- `frontend/src/features/analysis/Dashboard.jsx`
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-UI-SPEC.md`
- `.planning/phases/04-preprocessing-multi-stage-real-time-feedback-ui/SUMMARY.md`
</read_first>
<action>
Add `useState` for drawer open/close in `Dashboard.jsx`. Add primary button "Open Explainability" in the verdict banner area with `Brain` or `Sparkles` icon from `lucide-react`. Render `ExplainabilityDrawer` with `open`, `onClose`, and `result`. Do not remove or relocate existing overview panels or Phase 4 `EvidenceGraph` summary. Do not implement the Forensic Explanation tab (Plan 05-02).
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- `Dashboard.jsx` contains a button that sets drawer open state to true.
- `ExplainabilityDrawer` is rendered with the full `result` object.
- Existing overview layout (consensus, evidence summary, agents, timeline) remains on the dashboard.
- `EvidenceGraph` summary panel still receives `result.xai?.evidence_graph`.
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="6" name="Verify Plan 05-01">
<type>execute</type>
<files>
- `frontend/src/features/analysis/Dashboard.jsx`
</files>
<read_first>
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-01-PLAN.md`
- `frontend/package.json`
</read_first>
<action>
Run frontend production build and backend regression suite to confirm no Phase 4 or backend regressions. Fix only issues introduced by Plan 05-01 files.
</action>
<verify>
`python -m pytest tests\xai tests\consensus tests\integration\test_validation.py -q`
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- Backend pytest suite exits 0.
- `npm run build` exits 0.
- No Forensic Explanation tab components exist yet.
</acceptance_criteria>
</task>

</tasks>

<verification>
```powershell
. .venv\Scripts\Activate.ps1
python -m pytest tests\xai tests\consensus tests\integration\test_validation.py -q
Set-Location frontend
npm run build
```
</verification>

<must_haves>
- UI-02: Dashboard exposes an expandable explainability drawer.
- Drawer shows SHAP bar chart from `xai.shap_values.summary.average_values`.
- Drawer shows Level 1 counterfactual sensitivity controls with backend statements.
- Drawer shows interactive Plotly graph from `xai.evidence_graph`.
- Phase 4 overview evidence summary panel remains unchanged.
</must_haves>

<success_criteria>
- Investigator can open the drawer from the dashboard after analysis completes.
- SHAP, sensitivity, and Plotly sections render from live API payloads with graceful empty states.
- Production build passes without backend changes.
</success_criteria>

## PLANNING COMPLETE
