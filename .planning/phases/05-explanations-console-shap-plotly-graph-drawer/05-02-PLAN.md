---
phase: "05-explanations-console-shap-plotly-graph-drawer"
plan: "05-02"
title: "Detailed Forensic Explanation Tab"
type: "mvp"
wave: 2
depends_on: ["05-01"]
autonomous: true
requirements: ["UI-03"]
files_modified:
  - frontend/src/features/analysis/Dashboard.jsx
  - frontend/src/components/explainability/DashboardViewTabs.jsx
  - frontend/src/components/explainability/ForensicExplanationTab.jsx
  - frontend/src/components/explainability/NarrativeSections.jsx
  - frontend/src/components/explainability/ContradictionAlerts.jsx
  - frontend/src/components/explainability/EvidenceBreakdownTable.jsx
  - frontend/src/index.css
---

# Phase 5 Plan 05-02: Detailed Forensic Explanation Tab

<objective>
Deliver UI-03 as a vertical MVP slice: a dashboard tab presenting structured narrative sections, active contradiction/threat alerts, and tabular evidence breakdowns from existing `AnalysisResponse` fields — without backend changes.
</objective>

<tasks>

<task id="1" name="Dashboard view tabs">
<type>execute</type>
<files>
- `frontend/src/components/explainability/DashboardViewTabs.jsx`
- `frontend/src/features/analysis/Dashboard.jsx`
- `frontend/src/index.css`
</files>
<read_first>
- `frontend/src/components/layout/Sidebar.jsx`
- `frontend/src/features/analysis/Dashboard.jsx`
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-UI-SPEC.md`
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-01-PLAN.md`
</read_first>
<action>
Create `DashboardViewTabs.jsx` with local state or controlled props for `activeView` values `overview` and `explanation`. Render tab buttons "Overview" and "Forensic Explanation" with active styling class `.dashboard-tab-active`. Integrate into `Dashboard.jsx` below the verdict banner: when `overview`, render existing dashboard grid unchanged; when `explanation`, render placeholder for task 2. Preserve Explainability drawer from Plan 05-01 on both views.
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- `DashboardViewTabs.jsx` exports tab controls for `overview` and `explanation`.
- Default active view is `overview`.
- Overview view renders the same panels as pre-Phase-5 dashboard content.
- Explainability drawer button remains available in both views.
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="2" name="Structured narrative sections">
<type>execute</type>
<files>
- `frontend/src/components/explainability/NarrativeSections.jsx`
- `frontend/src/components/explainability/ForensicExplanationTab.jsx`
</files>
<read_first>
- `backend/explainability/narrative_engine.py`
- `backend/api/routes/analysis.py` (narrative payload assembly)
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-CONTEXT.md`
- `frontend/src/components/forensic/ConsensusPanel.jsx`
</read_first>
<action>
Create `NarrativeSections.jsx` that parses `result.narrative?.structured_summary` by splitting on markdown headers `## Finding`, `## Evidence`, `## Reliability`, `## Confidence`, `## Contradictions`, `## Explainability`. Render one `.glass` card per section with title and body text. Fallback: single card with `result.narrative?.human_summary`. Create `ForensicExplanationTab.jsx` container that mounts `NarrativeSections` as its first section.
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- Six section names from `NarrativeEngine.REQUIRED_SECTIONS` are supported when present in `structured_summary`.
- Fallback renders `human_summary` when structured summary is absent.
- `ForensicExplanationTab.jsx` renders `NarrativeSections` with the `result` prop.
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="3" name="Contradiction and threat alerts">
<type>execute</type>
<files>
- `frontend/src/components/explainability/ContradictionAlerts.jsx`
- `frontend/src/components/explainability/ForensicExplanationTab.jsx`
- `frontend/src/index.css`
</files>
<read_first>
- `backend/api/routes/analysis.py` (frontend_timeline threat_warnings)
- `backend/api/schemas/analysis.py` (`TimelineEventSchema.threat_warnings`)
- `frontend/src/components/forensic/FeatureAnalysisPanel.jsx`
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-UI-SPEC.md`
</read_first>
<action>
Create `ContradictionAlerts.jsx` that flattens `result.timeline[].threat_warnings` with chunk `start_time`/`end_time`, fields `type`, `severity`, `description`. Append rows from `result.diagnostics?.warnings` as secondary diagnostic alerts (no time range). Sort threat rows by severity (high before elevated before low). Add `.threat-alert` and severity modifier classes in `index.css`. Mount in `ForensicExplanationTab`.
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- Component lists at least one row per `timeline[].threat_warnings` entry when present.
- Each threat row shows severity, time range, type, and description.
- Diagnostic warnings from `diagnostics.warnings` appear when timeline threats are empty or alongside them.
- Empty state: "No active contradiction warnings for this report."
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="4" name="Evidence breakdown table">
<type>execute</type>
<files>
- `frontend/src/components/explainability/EvidenceBreakdownTable.jsx`
- `frontend/src/components/explainability/ForensicExplanationTab.jsx`
- `frontend/src/index.css`
</files>
<read_first>
- `backend/api/routes/analysis.py` (`_build_feature_analysis`)
- `frontend/src/components/forensic/FeatureAnalysisPanel.jsx`
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-RESEARCH.md`
</read_first>
<action>
Create `EvidenceBreakdownTable.jsx` with an HTML table (or stat-grid) showing acoustic feature rows from `result.feature_analysis?.acoustic_features` with columns Signal, Avg Z-Score, Severity, Risk Score. Add optional secondary table rows from `result.timeline[].details` agent entries (agent name, verdict, adjusted confidence) for the first chunk or a selected chunk. Style with `.evidence-table` in `index.css`. Mount in `ForensicExplanationTab`.
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- Table renders rows from `feature_analysis.acoustic_features` when available.
- Columns include signal name and `avg_z_score`.
- Agent detail rows can be derived from `timeline[].details` when present.
- Empty state: "No tabular evidence breakdown available."
- `ForensicExplanationTab` includes all three sub-panels: narrative, alerts, table.
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="5" name="Integrate explanation tab into Dashboard">
<type>execute</type>
<files>
- `frontend/src/features/analysis/Dashboard.jsx`
- `frontend/src/components/explainability/ForensicExplanationTab.jsx`
</files>
<read_first>
- `frontend/src/features/analysis/Dashboard.jsx`
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-02-PLAN.md`
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-01-PLAN.md`
</read_first>
<action>
Wire `ForensicExplanationTab` into `Dashboard.jsx` when `activeView === 'explanation'`. Pass full `result`. Ensure switching tabs does not clear drawer state or lose `result`. End-to-end path: upload via job flow → dashboard overview → open drawer (Plan 05-01) → switch to Forensic Explanation tab.
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- Selecting "Forensic Explanation" tab renders `ForensicExplanationTab`.
- Selecting "Overview" tab restores original dashboard grid.
- `result` prop is passed unchanged to explanation tab components.
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="6" name="Verify Plan 05-02 and milestone">
<type>execute</type>
<files>
- `frontend/src/features/analysis/Dashboard.jsx`
</files>
<read_first>
- `.planning/phases/05-explanations-console-shap-plotly-graph-drawer/05-VALIDATION.md`
- `.planning/ROADMAP.md`
</read_first>
<action>
Run full verification suite for Phase 5 completion. Fix regressions only in Phase 5 frontend files. Confirm UI-02 and UI-03 are satisfied together.
</action>
<verify>
`python -m pytest tests\xai tests\consensus tests\integration\test_validation.py -q`
`python -m backend.migrations.validate_schema`
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- All backend tests exit 0.
- Schema validation exits 0.
- Frontend build exits 0.
- Dashboard supports drawer (UI-02) and explanation tab (UI-03).
</acceptance_criteria>
</task>

</tasks>

<verification>
```powershell
. .venv\Scripts\Activate.ps1
python -m pytest tests\xai tests\consensus tests\integration\test_validation.py -q
python -m backend.migrations.validate_schema
Set-Location frontend
npm run build
```
</verification>

<must_haves>
- UI-03: Forensic Explanation tab renders structured narrative, threat alerts, and evidence tables.
- UI-02 drawer from Plan 05-01 remains functional alongside the new tab.
- End-to-end: upload → progress → dashboard → drawer + explanation tab consume persisted payloads.
- No backend API or schema changes required for this plan.
</must_haves>

<success_criteria>
- Investigator can switch to Forensic Explanation tab and read six narrative sections when available.
- Threat warnings and diagnostic alerts are visible with time context.
- Z-score and agent evidence tables render from existing response fields.
- Full project verification commands pass.
</success_criteria>

## PLANNING COMPLETE
