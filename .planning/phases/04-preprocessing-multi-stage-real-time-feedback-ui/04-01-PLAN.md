---
phase: "04-preprocessing-multi-stage-real-time-feedback-ui"
plan: "04-01"
title: "Evidence Graph and Real-Time Progress"
type: "mvp"
wave: 1
depends_on: []
autonomous: true
requirements: ["XAI-08", "UI-04"]
files_modified:
  - backend/explainability/evidence_graph.py
  - backend/api/routes/analysis.py
  - backend/api/schemas/analysis.py
  - frontend/src/App.jsx
  - frontend/src/features/analysis/Uploader.jsx
  - frontend/src/features/analysis/Dashboard.jsx
  - frontend/src/components/forensic/EvidenceGraph.jsx
  - frontend/src/index.css
  - tests/xai/test_evidence_graph.py
  - tests/integration/test_validation.py
---

# Phase 4 Plan 04-01: Evidence Graph and Real-Time Progress

<objective>
Deliver Phase 4 as a vertical MVP slice: generate and persist a deterministic 6-layer directed Evidence Graph, expose it in the analysis response, and replace simulated uploader progress with backend-reported real-time analysis stages.
</objective>

<tasks>

<task id="1" name="Build deterministic 6-layer Evidence Graph">
<type>execute</type>
<files>
- `backend/explainability/evidence_graph.py`
- `tests/xai/test_evidence_graph.py`
</files>
<read_first>
- `.planning/phases/04-preprocessing-multi-stage-real-time-feedback-ui/04-CONTEXT.md`
- `.planning/phases/04-preprocessing-multi-stage-real-time-feedback-ui/04-RESEARCH.md`
- `backend/explainability/evidence_graph.py`
- `backend/api/routes/analysis.py`
- `backend/persistence/database.py`
- `tests/xai/test_shap_engine.py`
- `tests/xai/test_counterfactual_engine.py`
</read_first>
<action>
Replace the placeholder graph builder with a deterministic JSON builder that exports `schema_version`, `layers`, `nodes`, `edges`, and `summary`. Represent the required layers as integers 1 through 6: Audio Input, Preprocessing/Reliability, Forensic Agents, Consensus Arbitration, XAI Artifacts, Verdict & Narrative. Add stable node IDs, typed node payloads, and directed relation strings including `processed_into`, `measured_by`, `evaluated_by`, `calibrated_into`, `explained_by`, `summarized_by`, and `supports_verdict`. Add pure tests proving all six layers exist, edges are directed, and threat warnings/XAI artifacts create graph nodes when present.
</action>
<verify>
`python -m pytest tests\xai\test_evidence_graph.py -q`
</verify>
<acceptance_criteria>
- `generate_evidence_graph(...)` returns a dict containing `schema_version`, `layers`, `nodes`, `edges`, and `summary`.
- The returned `layers` collection contains layer IDs `1`, `2`, `3`, `4`, `5`, and `6`.
- Every edge has `source`, `target`, and `relation`.
- `tests/xai/test_evidence_graph.py` asserts at least one node exists for each of the six required layers.
- `tests/xai/test_evidence_graph.py` exits 0.
</acceptance_criteria>
</task>

<task id="2" name="Persist and expose Evidence Graph in analysis response">
<type>execute</type>
<files>
- `backend/api/routes/analysis.py`
- `backend/api/schemas/analysis.py`
- `tests/integration/test_validation.py`
</files>
<read_first>
- `backend/api/routes/analysis.py`
- `backend/api/schemas/analysis.py`
- `backend/persistence/database.py`
- `backend/explainability/evidence_graph.py`
- `tests/integration/test_validation.py`
</read_first>
<action>
Import and call the Evidence Graph builder after `xai_payload` and `narrative_payload` are available. Persist the graph on the rich `XAIArtifact.evidence_graph` JSON field and include it in the final response under `xai.evidence_graph`. Keep `POST /analyze/` backward compatible and preserve existing response fields: `heatmap_base64`, `feature_analysis`, `diagnostics`, `xai.shap_values`, `xai.counterfactuals`, and `narrative`.
</action>
<verify>
`python -m pytest tests\integration\test_validation.py -q`
</verify>
<acceptance_criteria>
- `backend/api/routes/analysis.py` assigns `evidence_graph=` on the rich `XAIArtifact(...)` row.
- `final_response["xai"]` contains the key `"evidence_graph"`.
- `AnalysisResponse` accepts an `xai.evidence_graph` payload without validation errors.
- `tests/integration/test_validation.py` asserts `response.xai["evidence_graph"]["schema_version"]` exists.
</acceptance_criteria>
</task>

<task id="3" name="Add backend analysis job progress endpoints">
<type>execute</type>
<files>
- `backend/api/routes/analysis.py`
- `backend/api/schemas/analysis.py`
- `tests/integration/test_validation.py`
</files>
<read_first>
- `.planning/phases/04-preprocessing-multi-stage-real-time-feedback-ui/04-CONTEXT.md`
- `backend/api/routes/analysis.py`
- `backend/persistence/database.py`
- `backend/app.py`
- `tests/integration/test_validation.py`
</read_first>
<action>
Add a local in-memory analysis job store with stage updates and terminal states. Add `POST /analyze/jobs`, `GET /analyze/jobs/{job_id}/progress`, `GET /analyze/jobs/{job_id}/events`, and `GET /analyze/jobs/{job_id}/result`. Keep the existing synchronous `POST /analyze/` route by extracting the current analysis body into a reusable helper. Background analysis must create its own SQLAlchemy session via `SessionLocal`, update stages for upload received, preprocessing, segmentation, agent panel, consensus, XAI/evidence graph, narrative/persistence, complete, and error, and remove temp upload files in terminal paths.
</action>
<verify>
`python -m pytest tests\integration\test_validation.py -q`
</verify>
<acceptance_criteria>
- `backend/api/routes/analysis.py` defines routes for `/analyze/jobs`, `/analyze/jobs/{job_id}/progress`, `/analyze/jobs/{job_id}/events`, and `/analyze/jobs/{job_id}/result`.
- Progress payloads include `job_id`, `stage`, `status`, `percent`, `message`, and `error`.
- The background job path does not reuse the request-scoped `db` dependency session.
- Existing `POST /analyze/` remains available.
- Lightweight tests validate progress state transitions without loading neural models.
</acceptance_criteria>
</task>

<task id="4" name="Replace simulated uploader progress with backend-driven stages">
<type>execute</type>
<files>
- `frontend/src/App.jsx`
- `frontend/src/features/analysis/Uploader.jsx`
- `frontend/src/features/analysis/Dashboard.jsx`
- `frontend/src/components/forensic/EvidenceGraph.jsx`
- `frontend/src/index.css`
</files>
<read_first>
- `frontend/src/App.jsx`
- `frontend/src/features/analysis/Uploader.jsx`
- `frontend/src/features/analysis/Dashboard.jsx`
- `frontend/src/components/forensic/EvidenceGraph.jsx`
- `frontend/src/components/forensic/FeatureAnalysisPanel.jsx`
- `frontend/src/index.css`
- `frontend/package.json`
</read_first>
<action>
Update the upload flow so `App.jsx` starts analysis via `POST /analyze/jobs`, listens for progress through `EventSource` on `/analyze/jobs/{job_id}/events`, falls back to polling `/analyze/jobs/{job_id}/progress` if the stream errors, and fetches `/analyze/jobs/{job_id}/result` on completion. Update `Uploader.jsx` to accept a `progress` prop and render backend stage label, message, percent, completed stages, and terminal errors. Pass `result.xai?.evidence_graph` into `EvidenceGraph` from `Dashboard.jsx`; update `EvidenceGraph.jsx` to render the backend graph summary while keeping a graceful fallback for old responses. Add CSS in `index.css` for stage rows, active/completed/error states, and graph summary without creating the Phase 5 drawer or detailed explanation tab.
</action>
<verify>
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- `frontend/src/App.jsx` calls `/analyze/jobs` for new uploads.
- `frontend/src/App.jsx` uses `EventSource` for `/analyze/jobs/{job_id}/events` or falls back to polling `/progress`.
- `Uploader.jsx` no longer uses the fixed `animate={{ width: ['0%', '30%', '60%', '90%', '95%'] }}` progress source as the only progress indicator.
- `Dashboard.jsx` passes `result.xai?.evidence_graph` into `EvidenceGraph`.
- `EvidenceGraph.jsx` handles both graph payloads and old responses without throwing.
- `npm run build` exits 0.
</acceptance_criteria>
</task>

<task id="5" name="Run Phase 4 verification suite">
<type>execute</type>
<files>
- `tests/xai/test_evidence_graph.py`
- `tests/integration/test_validation.py`
</files>
<read_first>
- `.planning/phases/04-preprocessing-multi-stage-real-time-feedback-ui/04-01-PLAN.md`
- `tests/xai/test_evidence_graph.py`
- `tests/integration/test_validation.py`
- `backend/migrations/validate_schema.py`
- `frontend/package.json`
</read_first>
<action>
Run the backend XAI, consensus, and integration validation suite, run schema validation, and run the frontend production build. Fix any regressions introduced by Tasks 1 through 4 within the files listed in this plan. Do not add Phase 5 drawer/tab features while fixing verification failures.
</action>
<verify>
`python -m pytest tests\xai tests\consensus tests\integration\test_validation.py -q`
`python -m backend.migrations.validate_schema`
`npm run build` from `frontend/`
</verify>
<acceptance_criteria>
- `python -m pytest tests\xai tests\consensus tests\integration\test_validation.py -q` exits 0.
- `python -m backend.migrations.validate_schema` exits 0.
- `npm run build` from `frontend/` exits 0.
- No files under Phase 5 UI scope are created for explainability drawer or detailed explanation tab.
</acceptance_criteria>
</task>

</tasks>

<verification>
Run after implementation:

```powershell
. .venv\Scripts\Activate.ps1
python -m pytest tests\xai tests\consensus tests\integration\test_validation.py -q
python -m backend.migrations.validate_schema
Set-Location frontend
npm run build
```
</verification>

<must_haves>
- XAI-08: Response `xai.evidence_graph` contains a 6-layer directed JSON graph connecting audio input through verdict/narrative.
- UI-04: Uploader progress reflects backend-reported analysis stages from a progress endpoint or stream.
- Backward compatibility: Existing `POST /analyze/` response fields remain available.
- Phase boundary: No Phase 5 explainability drawer or detailed explanation tab is implemented in this phase.
</must_haves>

<success_criteria>
- Evidence graph is generated, persisted, and exposed in API responses.
- Backend progress endpoint/stream reports real stage updates during analysis jobs.
- Frontend uploader renders backend-driven progress and routes to the dashboard when the job completes.
- Lightweight tests cover graph shape, response schema, and progress state contracts.
- Backend and frontend verification commands pass.
</success_criteria>

## PLANNING COMPLETE
